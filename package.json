{
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "prestart": "./scripts/prebuild",
    "start": "concurrently -k \"tsc --watch\" \"elm-spa server\"",
    "prebuild": "./scripts/prebuild",
    "build": "tsc && elm-spa build"
  },
  "dependencies": {
    "@cooklang/cooklang-ts": "^1.0.0"
  },
  "devDependencies": {
    "concurrently": "^7.4.0",
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-spa": "^6.0.4",
    "typescript": "^4.8.3"
  }
}