{
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "prestart": "./scripts/prebuild",
    "start": "elm-spa server",
    "prebuild": "./scripts/prebuild",
    "build": "elm-spa build"
  },
  "dependencies": {
    "@cooklang/cooklang-ts": "^1.0.0"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-spa": "^6.0.4"
  }
}